#include <iostream>
using namespace std;

int main(){
float a,b,c;

cout << "Enter three numbers: ";
cin >> a >> b >> c;

if ((a<=b)&&(b<=c)){
    cout << "Numbers in non-descending order";
}
else{ cout << "Numbers aren't in non-descending order";}

}
