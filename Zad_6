#include <iostream>
#include<time.h>
using namespace std;

int main(){

int Osize;
int Tsize;
bool flag;
int cnt=0;

cin >> Osize;
cin >> Tsize;

char *one = new char[Osize];
char *two = new char[Tsize];

for(int i=0; i<Osize; i++)
    cin >>*(one+i);

for(int j=0; j<Tsize; j++)
    cin >>*(two+j);

if (Osize > Tsize)
   cout << "Nie zawiera sie";

else{
for(int i=0; i<Osize; i++){

    for(int j=0; j<Tsize; j++){
        flag=0;
        cnt=0;
        if(*(one+i)==*(two+j)){
               flag=1;
        for(int p=i, k=j; p<Osize && k<Tsize; p++, k++ ){
            if(*(one+p)!= *(two+k)){
                flag=0;
                break;
            }
            cnt++;
        }
        }
        if((flag==1)&&(cnt==Osize))
            break;
    }
    if((flag==1)&&(cnt==Osize))
        break;
}

if((flag==1)&&(cnt==Osize))
    cout << "Zawiera sie";
    else
    cout << "Nie zawiera sie";
}
}
